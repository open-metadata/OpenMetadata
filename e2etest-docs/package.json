{
  "name": "e2e-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "yarn generate && vitepress dev docs",
    "dev": "yarn run generate && vitepress dev docs",
    "build": "yarn run generate && vitepress build docs",
    "preview": "vitepress preview docs",
    "generate": "npx tsx scripts/generate.ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitepress": "^1.0.0-rc.31",
    "vue": "^3.4.0"
  }
}
