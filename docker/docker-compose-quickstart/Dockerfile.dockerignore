# Allowlist-style ignore for docker/docker-compose-quickstart/Dockerfile, same
# shape as docker/storybook/Dockerfile.dockerignore. This image downloads the
# release tarball rather than copying it, so the only thing it needs out of the
# repo is the HEALTHCHECK probe source.

# Exclude everything.
**

!docker/
!docker/Health.java

# The entrypoint script.
!docker/openmetadata-start.sh
