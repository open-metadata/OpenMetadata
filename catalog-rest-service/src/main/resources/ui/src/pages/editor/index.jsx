import React from 'react';
import MarkdownEditor from '../../components/common/editor/Editor';

const Editor = () => {
  return <MarkdownEditor placeholder="Write some rich text..." />;
};

export default Editor;
