{
  "$id": "https://open-metadata.org/schema/entity/services/connections/database/customMlModelConnection.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CustomMlModelConnection",
  "description": "Custom MlModel Service connection to build a source that is not supported by OpenMetadata yet.",
  "type": "object",
  "javaType": "org.openmetadata.catalog.services.connections.mlmodel.CustomMlModelConnection",
  "definitions": {
    "customMlModelType": {
      "type": "string",
      "enum": ["CustomMlModel"],
      "default": "CustomMlModel"
    }
  },
  "properties": {
    "type": {
      "title": "Service Type",
      "description": "Custom Ml model service type",
      "$ref": "#/definitions/customMlModelType",
      "default": "CustomMlModel"
    },
    "config": {
      "description": "Additional connection options to build the URL that can be sent to service during the connection.",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "additionalProperties": false,
  "required": ["sourcePythonClass", "type"]
}
