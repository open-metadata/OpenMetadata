{
    "$id": "https://open-metadata.org/schema/tests/table/tableColumnToMatchSet.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "TableColumnToMatchSet",
    "description": "This schema defines the test TableColumnToMatchSet. Test the table columns match a set of values. Unordered by default",
    "type": "object",
    "javaType": "org.openmetadata.catalog.tests.table.TableColumnToMatchSet",
    "properties": {
      "columnNames": {
        "description": "Expected columns of the table to match the ones in {columnValuesSet}",
        "type": "string"
      },
      "ordered": {
        "description": "Weather or not to considered the order of the list when performing the match",
        "type": "boolean",
        "default": false
      }
    },
    "required": ["columnNames"],
    "additionalProperties": false
  }
  