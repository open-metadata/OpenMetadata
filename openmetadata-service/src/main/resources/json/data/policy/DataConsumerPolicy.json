{
  "name": "DataConsumerPolicy",
  "displayName": "Data Consumer Policy",
  "fullyQualifiedName": "DataConsumerPolicy",
  "description": "Policy for Data Consumer to perform operations on metadata entities",
  "enabled": true,
  "allowDelete": false,
  "provider": "system",
  "rules": [
    {
      "name": "DataConsumerPolicy-EditRule",
      "description" : "Allow some of the edit operations on a resource for everyone.",
      "resources" : ["all"],
      "operations": ["ViewAll", "EditDescription", "EditTags", "EditGlossaryTerms", "EditTier", "EditCertification"],
      "effect": "allow"
    },
    {
      "name": "DataConsumerPolicy-CreateTaskResource-Rule",
      "description" : "Resource-level grant that keeps the legacy `task.Create` permission so existing UI gates continue to render task-creation affordances (e.g. the Request Data Access button). Real per-entity enforcement happens via the entity-scoped CreateTask/EditTask rule below.",
      "resources" : ["task"],
      "operations": ["Create"],
      "effect": "allow"
    },
    {
      "name": "DataConsumerPolicy-TaskRule",
      "description" : "Allow authenticated users to file and edit tasks (data access requests, suggestions, etc.) against any entity. Restrict this rule (e.g. with an isOwner condition) to limit who can file or edit tasks on which entities.",
      "resources" : ["all"],
      "operations": ["CreateTask", "EditTask"],
      "effect": "allow"
    }
  ]
}
