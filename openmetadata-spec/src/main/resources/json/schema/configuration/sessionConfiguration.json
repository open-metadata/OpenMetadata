{
  "$id": "https://open-metadata.org/schema/entity/configuration/sessionConfiguration.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SessionConfiguration",
  "description": "This schema defines the Session Configuration for managing session cookies and related settings.",
  "type": "object",
  "javaType": "org.openmetadata.schema.configuration.SessionConfiguration",
  "properties": {
    "forceSecureSessionCookie": {
      "description": "Force secure flag on session cookies even when not using HTTPS directly. Enable this when running behind a proxy/load balancer that handles SSL termination.",
      "type": "boolean",
      "default": false
    }
  },
  "additionalProperties": false
}