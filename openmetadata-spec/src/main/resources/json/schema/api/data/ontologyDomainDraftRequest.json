{
  "$id": "https://open-metadata.org/schema/api/data/ontologyDomainDraftRequest.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OntologyDomainDraftRequest",
  "description": "Generate a reviewable ontology Draft without applying any entity mutation.",
  "type": "object",
  "javaType": "org.openmetadata.schema.api.data.OntologyDomainDraftRequest",
  "properties": {
    "glossary": {
      "$ref": "../../type/basic.json#/definitions/fullyQualifiedEntityName"
    },
    "changeSetName": {
      "$ref": "../../type/basic.json#/definitions/entityName"
    },
    "displayName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "description": {
      "$ref": "../../type/basic.json#/definitions/markdown"
    },
    "domainDescription": {
      "type": "string",
      "minLength": 1,
      "maxLength": 10000
    },
    "maxConcepts": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 25
    },
    "proposal": {
      "description": "Already-extracted structured proposal. When present, the server compiles it without asking another model to reinterpret it. Requires discoveryContext.",
      "$ref": "../../type/ontologyDiscoveryProposal.json"
    },
    "discoveryContext": {
      "description": "Optional service-discovery provenance. The server adds the actual model identifier and generation time to the persisted draft.",
      "$ref": "../../type/ontologyDiscoveryContext.json"
    }
  },
  "required": [
    "glossary",
    "changeSetName",
    "displayName",
    "description",
    "domainDescription",
    "maxConcepts"
  ],
  "additionalProperties": false
}
