{
  "$id": "https://open-metadata.org/schema/events/alertShadowDisagreement.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AlertShadowDisagreement",
  "description": "One event about which the stored condition text and the plan gave different answers.",
  "type": "object",
  "javaType": "org.openmetadata.schema.entity.events.AlertShadowDisagreement",
  "properties": {
    "eventId": {
      "$ref": "../type/basic.json#/definitions/uuid"
    },
    "offset": {
      "description": "Offset of the event in the change event table.",
      "type": "integer",
      "existingJavaType": "Long"
    },
    "subjectType": {
      "description": "Type of the entity the event is about.",
      "type": "string"
    },
    "storedText": {
      "description": "What the stored condition text answered: yes, no, or the exception it threw.",
      "type": "string"
    },
    "plan": {
      "description": "What the plan answered: yes, no, or the exception it threw.",
      "type": "string"
    },
    "timestamp": {
      "$ref": "../type/basic.json#/definitions/timestamp"
    }
  },
  "required": ["eventId", "storedText", "plan", "timestamp"],
  "additionalProperties": false
}
