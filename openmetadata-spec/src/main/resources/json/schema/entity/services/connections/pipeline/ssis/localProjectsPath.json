{
    "$id": "https://open-metadata.org/schema/entity/services/connections/pipeline/ssis/localProjectsPath",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Local Projects Path",
    "description": "Local Projects Path for SSIS.",
    "type": "object",
    "javaType": "org.openmetadata.schema.services.connections.pipeline.ssis.localProjectsPath",
    "definitions": {
      "localType": {
        "description": "Local storage service type",
        "type": "string",
        "enum": ["local"],
        "default": "local"
      }
    },
    "properties": {
      "type": {
        "title": "Service Type",
        "description": "Service Type",
        "$ref": "#/definitions/localType",
        "default": "local",
        "readOnly": true
      },
      "projectsPath": {
        "title": "Projects Path",
        "description": "Path leading to your projects",
        "type": "string"
      }
    },
    "additionalProperties": false,
    "required": [
      "projectsPath"
    ]
  }