{
  "$id": "https://open-metadata.org/schema/entity/applications/retentionJobContext.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Retention Job Context",
  "javaType": "org.openmetadata.schema.entity.app.RetentionJobContext",
  "description": "Retention Job Context",
  "type": "object",
  "properties": {
    "entity": {
      "description": "The entity for which the retention policy applies",
      "type": "string",
      "example": "EventSubscription"
    },
    "retentionPeriod": {
      "title": "Retention Period",
      "description": "Select the retention period after which the records will be deleted",
      "$ref": "../applications/configuration/internal/retentionPolicyConfiguration.json#/properties/retentionPeriod"
    },
    "startedAt": {
      "description": "The timestamp when the retention policy started",
      "type": "string",
      "format": "date-time"
    },
    "comingRetentionDate": {
      "description": "The timestamp for the next retention execution",
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["entity", "retentionPeriod", "startedAt", "comingRetentionDate"],
  "additionalProperties": false
}