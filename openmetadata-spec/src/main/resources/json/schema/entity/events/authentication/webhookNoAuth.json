{
  "$id": "https://open-metadata.org/schema/entity/events/webhookNoAuth.json",
  "title": "None",
  "description": "No authentication.",
  "type": "object",
  "properties": {
    "type": {
      "description": "Authentication type discriminator.",
      "type": "string",
      "enum": ["none"],
      "default": "none"
    }
  },
  "required": ["type"],
  "additionalProperties": false
}