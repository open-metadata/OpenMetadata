# Enable parallel execution at class and method level
junit.jupiter.execution.parallel.enabled = true
junit.jupiter.execution.parallel.mode.default = concurrent
junit.jupiter.execution.parallel.mode.classes.default = concurrent

# Local runs keep adapting to available processors; CI invocations override this with a fixed,
# reported worker count so failures are reproducible without slowing high-core development hosts.
junit.jupiter.execution.parallel.config.strategy = dynamic
junit.jupiter.execution.parallel.config.dynamic.factor = 1.0

# Ensure test instances are created per method for proper isolation
junit.jupiter.testinstance.lifecycle.default = per_method
