{
  "name": "open-metadata",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=10.0.0",
    "yarn": "^1.22.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/open-metadata/OpenMetadata.git"
  },
  "devDependencies": {
    "node-gyp": "^10.0.1",
    "quicktype": "^23.0.170"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
