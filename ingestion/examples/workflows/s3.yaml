source:
  type: s3
  config:
    service_name: aws-s3
sink:
  type: metadata-rest
  config: {}
metadata_server:
  type: metadata-server
  config:
    api_endpoint: http://localhost:8585/api
    auth_provider_type: no-auth
