source:
  type: datalake
  serviceName: local_datalake
  serviceConnection:
    config:
      type: Datalake
      configSource:
        awsConfig:        #gcsConfig and awsConfig here
          awsAccessKeyId: aws access key id
          awsSecretAccessKey: aws secret access key
          awsRegion: region
      bucketName: bucket name
      
  sourceConfig:
    config:
      tableFilterPattern:
        includes:
        - ''
sink:
  type: metadata-rest
  config: {}
workflowConfig:
  openMetadataServerConfig:
    hostPort: http://localhost:8585/api
    authProvider: no-auth
  