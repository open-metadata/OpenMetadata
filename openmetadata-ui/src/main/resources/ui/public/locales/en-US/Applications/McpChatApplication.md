# MCP Chat

AI-powered chat assistant for OpenMetadata. Uses MCP tools to search, explore, and manage metadata through natural language. The LLM provider and credentials are configured at the platform level via llmConfiguration.

$$section
### System Prompt $(id="systemPrompt")

The system prompt that guides the assistant's behavior.

$$